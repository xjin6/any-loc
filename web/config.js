/* AnyLoc 配置文件
 * ================
 * 在这里填你自己的高德地图 Web服务 key，搜索就会用高德（中国地名/小区/店铺最全）。
 * 留空则自动回退到 OpenStreetMap（免 key，但中国数据少）。
 *
 * 怎么拿 key：
 *   1. 打开 https://lbs.amap.com/  注册/登录（需实名）
 *   2. 控制台 → 应用管理 → 创建新应用 → 添加 Key
 *   3. 服务平台务必选「Web服务」
 *   4. 把生成的 key 粘贴到下面引号里
 */
window.ANYLOC_CONFIG = {
  // ← 把你的高德 Web服务 key 填在这里（引号中间）
  amapKey: "",
};
